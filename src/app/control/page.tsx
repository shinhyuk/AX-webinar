import { ControlConsole } from "@/components/control/ControlConsole";

export const metadata = {
  title: "운영자 콘솔 | HR-AX Q&A",
};

export default function ControlPage() {
  return <ControlConsole />;
}
